{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/((?!assets|android|ios|windows11|manifest|registerSW|logoUtl|favicon|robots).*)",
            "destination": "/index.html"
        }
    ]
}