{
    "routes": [
        {
            "handle": "filesystem"
        },
        {
            "src": "/((?!assets|android|ios|windows11|manifest|registerSW|logoUtl|favicon|robots).*)",
            "dest": "/index.html"
        }
    ]
}